# Copy to app/.env.local only when Milestone 2 supplies authenticated sync.
# These PUBLIC browser settings do not enable cloud synchronization in Milestone 0.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
# Never place service-role, secret, database-password or access-token values here.

