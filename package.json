{
  "name": "rrwrs-system",
  "private": true,
  "version": "0.1.0",
  "workspaces": ["app"],
  "engines": { "node": ">=22.12.0 <25", "npm": ">=10" },
  "scripts": {
    "dev": "npm run dev --workspace app",
    "dev:demo": "npm run dev:demo --workspace app",
    "typecheck": "npm run typecheck --workspace app",
    "lint": "npm run lint --workspace app",
    "test": "npm run test --workspace app",
    "test:e2e": "npm run test:e2e --workspace app",
    "build": "npm run build --workspace app",
    "build:demo": "npm run build:demo --workspace app",
    "preview": "npm run preview --workspace app",
    "check": "npm run typecheck && npm run lint && npm test && npm run build"
  }
}
